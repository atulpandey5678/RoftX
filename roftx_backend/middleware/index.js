// roftx-platform: Express middleware
//
// This directory will hold the middleware modules described in the design's
// Module Layout:
//   - auth.js -> signSessionToken, authenticateToken, ownership helpers
//
// Placeholder index so the directory exists in the repo. Real modules are
// implemented in later tasks (see .kiro/specs/roftx-platform/tasks.md, task 3+).

export {};
